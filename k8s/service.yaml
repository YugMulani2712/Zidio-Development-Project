
apiVersion: v1
kind: Service
metadata:
  name: retailpulse-service
spec:
  type: NodePort
  selector:
    app: retailpulse
  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30085
